// The generated module is exported from agents-city-web with a source manifest.
// Keep this facade small so the public CLI cannot drift onto a second crypto stack.
export {
  ConnectApiError,
  beginDeviceAuthorization,
  listDeviceRoads,
  pollDeviceAuthorization,
  signedRelayHeaders,
  syncDeviceCities,
  type DeviceAuthorization,
  type DeviceIdentity,
  type DeviceKeys,
  type DeviceRoad,
  type KeyTransparencyTrust,
} from '../managed-connect-client.js';
